# WARNING: These credentials are for LOCAL DEVELOPMENT ONLY.
# Change all passwords and secrets before using in any shared or production environment.

name: testapp

services:
  mysql:
    image: mysql:8.0
    container_name: testapp-mysql
    restart: unless-stopped
    ports:
      - '${MYSQL_PORT:-3389}:3306'
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: testapp_db
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - testapp-network
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']
      interval: 10s
      timeout: 5s
      retries: 5

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: testapp-phpmyadmin
    restart: unless-stopped
    ports:
      - '${PHPMYADMIN_PORT:-8163}:80'
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: rootpassword
      PMA_ARBITRARY: 0
      HIDE_PHP_VERSION: 'true'
    volumes:
      - ./phpmyadmin-config.php:/etc/phpmyadmin/config.user.inc.php
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - testapp-network

  redis:
    image: redis:7-alpine
    container_name: testapp-redis
    restart: unless-stopped
    ports:
      - '${REDIS_PORT:-6462}:6379'
    volumes:
      - redis_data:/data
    networks:
      - testapp-network
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: testapp-redis-commander
    restart: unless-stopped
    ports:
      - '${REDIS_COMMANDER_PORT:-8164}:8081'
    environment:
      REDIS_HOSTS: local:redis:6379
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - testapp-network

volumes:
  mysql_data:
  redis_data:

networks:
  testapp-network:
    driver: bridge
